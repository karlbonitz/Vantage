# packager output / OS noise
*.zip
.DS_Store
Thumbs.db

# packager local dry-run output
.release/

# Embedded libraries are fetched by the packager (.pkgmeta externals). A local
# clone for in-game testing can live in Libs/, but it's never committed.
Libs/
